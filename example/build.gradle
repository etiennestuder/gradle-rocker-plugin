plugins {
    id 'nu.studer.rocker' version '2.0'
    id 'java'
}

repositories {
    jcenter()
}

rockerVersion = '1.2.2'

rocker {
    main {
        optimize = true
        templateDir = file('src/rocker')
        outputDir = file('src/generated/rocker')
    }
}
