plugins {
    id 'nu.studer.rocker' version '2.2'
    id 'java'
}

repositories {
    jcenter()
}

rockerVersion = '1.3.0'

rocker {
    main {
        optimize = true
        templateDir = file('src/rocker')
        outputDir = file('src/generated/rocker')
    }
}
